// AES-256 Core Implementation
// Since AES-GCM uses CTR mode, we only need the Forward cipher (Encryption).
// Decryption in CTR mode is just encryption of the counter, XORed with ciphertext.

const SBOX = new Uint8Array([
  
]);

const RCON = new Uint32Array([
  0x00000000, 0x01000000, 0x02000000, 0x04000000, 0x08000000, 0x10000000,
  0x20000000, 0x40000000, 0x80000000, 0x1b000000, 0x36000000
]);

/**
 * AES-256 uses a 32-byte key and expands it to 60 words (15 round keys of 4 words each).
 */
export function expandKey256(key: Uint8Array): Uint32Array {
  if (key.length !== 32) {
    throw new Error('AES-256 needs a 32-byte key.');
  }
  const w = new Uint32Array(60);
  for (let i = 0; i < 8; i++) {
    w[i] = (key[4 * i] << 24) | (key[4 * i + 1] << 16) | (key[4 * i + 2] << 8) | key[4 * i + 3];
  }
  for (let i = 8; i < 60; i++) {
    let temp = w[i - 1];
    if (i % 8 === 0) {
      temp = subWord(rotWord(temp)) ^ RCON[i / 8];
    } else if (i % 8 === 4) {
      temp = subWord(temp);
    }
    w[i] = w[i - 8] ^ temp;
  }
  return w;
}

function subWord(word: number): number {
  return (SBOX[(word >>> 24) & 0xff] << 24) |
         (SBOX[(word >>> 16) & 0xff] << 16) |
         (SBOX[(word >>> 8) & 0xff] << 8) |
         (SBOX[word & 0xff]);
}

function rotWord(word: number): number {
  return (word << 8) | (word >>> 24);
}

// GF(2^8) math for MixColumns
function gmult(a: number, b: number): number {
  let p = 0;
  for (let i = 0; i < 8; i++) {
    if ((b & 1) !== 0) p ^= a;
    let hiBitSet = (a & 0x80) !== 0;
    a <<= 1;
    if (hiBitSet) a ^= 0x11b; // x^8 + x^4 + x^3 + x + 1
    b >>>= 1;
  }
  return p & 0xff;
}

/**
 * AddRoundKey: XORs the state with the round key.
 */
function addRoundKey(state: Uint8Array, w: Uint32Array, round: number): void {
  for (let c = 0; c < 4; c++) {
    const word = w[round * 4 + c];
    state[c * 4 + 0] ^= (word >>> 24) & 0xff;
    state[c * 4 + 1] ^= (word >>> 16) & 0xff;
    state[c * 4 + 2] ^= (word >>> 8) & 0xff;
    state[c * 4 + 3] ^= word & 0xff;
  }
}

/**
 * SubBytes: Applies S-box to each byte.
 */
function subBytes(state: Uint8Array): void {
  for (let i = 0; i < 16; i++) {
    state[i] = SBOX[state[i]];
  }
}

/**
 * ShiftRows: Shifts rows 1, 2, 3 by 1, 2, 3 respectively.
 */
function shiftRows(state: Uint8Array): void {
  const t = new Uint8Array(state);
  
  // Row 0 - no shift
  state[0] = t[0]; state[4] = t[4]; state[8] = t[8]; state[12] = t[12];
  
  // Row 1 - shift left 1
  state[1] = t[5]; state[5] = t[9]; state[9] = t[13]; state[13] = t[1];
  
  // Row 2 - shift left 2
  state[2] = t[10]; state[6] = t[14]; state[10] = t[2]; state[14] = t[6];
  
  // Row 3 - shift left 3
  state[3] = t[15]; state[7] = t[3]; state[11] = t[7]; state[15] = t[11];
}

/**
 * MixColumns: Mixes columns 0, 1, 2, 3 individually.
 */
function mixColumns(state: Uint8Array): void {
  for (let c = 0; c < 4; c++) {
    const s0 = state[c * 4 + 0];
    const s1 = state[c * 4 + 1];
    const s2 = state[c * 4 + 2];
    const s3 = state[c * 4 + 3];

    state[c * 4 + 0] = gmult(s0, 2) ^ gmult(s1, 3) ^ s2 ^ s3;
    state[c * 4 + 1] = s0 ^ gmult(s1, 2) ^ gmult(s2, 3) ^ s3;
    state[c * 4 + 2] = s0 ^ s1 ^ gmult(s2, 2) ^ gmult(s3, 3);
    state[c * 4 + 3] = gmult(s0, 3) ^ s1 ^ s2 ^ gmult(s3, 2);
  }
}

/**
 * Encrypts a single 16-byte block.
 * @param state Input/Output block (16 bytes Uint8Array). Mutates in place.
 * @param w the expanded key words (Uint32Array of length 60).
 */
export function encryptBlock(state: Uint8Array, w: Uint32Array): void {
  // AddRoundKey Base
  addRoundKey(state, w, 0);

  // 13 rounds of normal operations
  for (let round = 1; round < 14; round++) {
    subBytes(state);
    shiftRows(state);
    mixColumns(state);
    addRoundKey(state, w, round);
  }

  // Final round 14 (No MixColumns)
  subBytes(state);
  shiftRows(state);
  addRoundKey(state, w, 14);
}
